import React from "react";

interface Props {
  name: string | null;
  surname: string | null;
  email: string | null;
  phone: string | null;
}

export default function ProfileChange({ name, surname, email, phone }: Props) {
  return <></>;
}
